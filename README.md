# Udhaar Ledger

Simple offline-first udhaar ledger app for small shopkeepers.

## Release

Play Store upload uses:

```powershell
flutter build appbundle --release
```

See `STORE_LISTING_CHECKLIST.md` before publishing.

Do not commit `android/key.properties` or `android/upload-keystore.jks`.
